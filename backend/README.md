# Backend API

Express + MongoDB API for authentication and expense management.

## Setup

1. Copy `.env.example` to `.env`
2. Install dependencies: `npm install`
3. Run in development: `npm run dev`
